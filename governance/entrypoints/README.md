# Governance Entrypoints

This directory defines **Layer 1 / UI entry** metadata in a reusable way.

Each file here describes a slash command or UI entrypoint (for example `/plan-*`, `/research-*`),
how it should be interpreted, and which governance router it should delegate to.
