@startuml Config-Sync Sequence Diagram

!theme plain
title Config-Sync System: Time Sequence Flow
scale 1.0

' Define styles with better contrast
skinparam actor {
    BackgroundColor #4A90E2
    BorderColor #2E5C8A
    FontColor White
}

skinparam participant {
    BackgroundColor #52C41A
    BorderColor #389E0D
    FontColor White
}

skinparam database {
    BackgroundColor #FA8C16
    BorderColor #D46B08
    FontColor White
}

skinparam sequence {
    ArrowColor #333333
    ArrowFontColor #333333
    LifeLineBorderColor #333333
    LifeLineBackgroundColor #F5F5F5
}

skinparam note {
    BackgroundColor #F0F0F0
    BorderColor #999999
    FontColor #333333
}

' Actors and Participants
actor "User" as user
participant "config-sync CLI" as cli
participant "Permission Adapter" as perm_adapter
participant "Memory Adapter" as memory_adapter
participant "Droid Adapter" as droid
participant "Qwen Adapter" as qwen
participant "Codex Adapter" as codex
participant "OpenCode Adapter" as opencode

participant "Claude Config" as claude_config
participant "Droid CLI" as droid_tool
participant "Qwen CLI" as qwen_tool
participant "Codex CLI" as codex_tool
participant "OpenCode" as opencode_tool

' == INITIAL SETUP SEQUENCE ==
autonumber 1

group Initial Setup - Sync All Tools
    user -> cli: /config-sync/sync-cli --action=sync --target=all --components=all
    activate cli

    note over cli
      parse_target_list("all") ⇒ droid,qwen,codex,opencode
      parse_component_list("all") ⇒ commands,rules,settings,permissions,memory
    end note

    cli -> claude_config: Read configuration files
    activate claude_config
    claude_config --> cli: rules, commands, settings, memory
    deactivate claude_config

    note over cli: Create backup directories\nfor all target tools

    par Parallel Sync to Multiple Tools
        cli -> droid: Sync rules & commands\n(excludes config-sync module)
        activate droid

        cli -> qwen: Sync rules & commands\n(excludes config-sync module)
        activate qwen

        cli -> codex: Sync rules & commands\n(excludes config-sync module)
        activate codex

        cli -> opencode: Sync rules & commands\n(excludes config-sync module)
        activate opencode
    end

    == Permission Adaptation ==
    cli -> perm_adapter: Adapt permissions for all tools
    activate perm_adapter

    par Parallel Permission Processing
        perm_adapter -> droid: Map to allowlist/denylist
        perm_adapter -> qwen: Generate permission guidelines
        perm_adapter -> codex: Set sandbox levels
        perm_adapter -> opencode: Map to operation-based
    end

    perm_adapter --> cli: Permission adaptation complete
    deactivate perm_adapter

    == Memory Synchronization ==
    cli -> memory_adapter: Sync memory files
    activate memory_adapter

    par Parallel Memory Processing
        memory_adapter -> droid: Create DROID.md + AGENTS.md
        memory_adapter -> qwen: Create QWEN.md + AGENTS.md
        memory_adapter -> codex: Create CODEX.md + AGENTS.md
        memory_adapter -> opencode: Create OPENCODE.md + AGENTS.md
    end

    memory_adapter --> cli: Memory sync complete
    deactivate memory_adapter

    == Complete Tool Adapters ==
    droid -> droid_tool: Write ~/.factory/ configuration
    activate droid_tool
    droid_tool --> droid: Success
    deactivate droid_tool
    droid --> cli: Droid sync complete
    deactivate droid

    qwen -> qwen_tool: Write ~/.qwen/ configuration
    activate qwen_tool
    qwen_tool --> qwen: Success
    deactivate qwen_tool
    qwen --> cli: Qwen sync complete
    deactivate qwen

    codex -> codex_tool: Write ~/.codex/ configuration
    activate codex_tool
    codex_tool --> codex: Success
    deactivate codex_tool
    codex --> cli: Codex sync complete
    deactivate codex

    opencode -> opencode_tool: Write ~/.config/opencode/ configuration
    activate opencode_tool
    opencode_tool --> opencode: Success
    deactivate opencode_tool
    opencode --> cli: OpenCode sync complete
    deactivate opencode

    cli --> user: ✅ All tools synchronized successfully
    deactivate cli
end

' == VERIFICATION SEQUENCE ==
group Verification Phase
    user -> cli: /config-sync/sync-cli --action=verify --target=all --components=all --detailed
    activate cli

    note over cli
      Users can provide comma-separated target/component lists
      parse_target_list()/parse_component_list() expand before verification
    end note

    par Parallel Verification
        cli -> droid: Verify configuration integrity
        activate droid
        droid -> droid_tool: Check files exist & valid
        activate droid_tool
        droid_tool --> droid: Verification results
        deactivate droid_tool
        droid --> cli: Droid verification complete
        deactivate droid

        cli -> qwen: Verify configuration integrity
        activate qwen
        qwen -> qwen_tool: Check files exist & valid
        activate qwen_tool
        qwen_tool --> qwen: Verification results
        deactivate qwen_tool
        qwen --> cli: Qwen verification complete
        deactivate qwen

        cli -> codex: Verify configuration integrity
        activate codex
        codex -> codex_tool: Check files exist & valid
        activate codex_tool
        codex_tool --> codex: Verification results
        deactivate codex_tool
        codex --> cli: Codex verification complete
        deactivate codex

        cli -> opencode: Verify configuration integrity
        activate opencode
        opencode -> opencode_tool: Check files exist & valid
        activate opencode_tool
        opencode_tool --> opencode: Verification results
        deactivate opencode_tool
        opencode --> cli: OpenCode verification complete
        deactivate opencode
    end

    cli --> user: 📊 Verification report with results
    deactivate cli
end

' == UPDATE SEQUENCE ==
group Targeted Update
    user -> cli: /config-sync/sync-cli --action=sync --target=droid --components=permissions
    activate cli

    cli -> claude_config: Read current permissions
    activate claude_config
    claude_config --> cli: Permission settings
    deactivate claude_config

    cli -> perm_adapter: Adapt permissions for Droid
    activate perm_adapter
    perm_adapter -> droid: Map permissions to Droid format
    activate droid
    droid -> droid_tool: Update ~/.factory/settings.json
    activate droid_tool
    droid_tool --> droid: Update successful
    deactivate droid_tool
    droid --> perm_adapter: Permissions applied
    deactivate droid
    perm_adapter --> cli: Permission adaptation complete
    deactivate perm_adapter

    cli --> user: ✅ Droid permissions updated
    deactivate cli
end

' == ANALYSIS SEQUENCE ==
group Analysis Phase
    user -> cli: /config-sync/sync-cli --action=analyze --target=all --format=table
    activate cli

    par Parallel Analysis
        cli -> droid: Check installation & config
        activate droid
        droid --> cli: Droid status report
        deactivate droid

        cli -> qwen: Check installation & config
        activate qwen
        qwen --> cli: Qwen status report
        deactivate qwen

        cli -> codex: Check installation & config
        activate codex
        codex --> cli: Codex status report
        deactivate codex

        cli -> opencode: Check installation & config
        activate opencode
        opencode --> cli: OpenCode status report
        deactivate opencode
    end

    cli --> user: 📋 Analysis table with tool status
    deactivate cli
end

' Notes for timing and duration
note over user, opencode_tool
    **Timing Estimates**
    • Initial setup: 2-5 minutes
    • Targeted update: 30-60 seconds
    • Verification: 1-2 minutes
    • Analysis: 30-60 seconds

    **Parallel Processing**
    • All tool adapters run in parallel
    • Reduces total sync time significantly
    • Independent tool operations
end note

note right of perm_adapter
    **Permission Mapping Logic**
    • Claude allow/ask/deny → Tool-specific formats
    • Security-first approach
    • Risk assessment for each tool
    • Format conversion: JSON → TOML → Guidelines
end note

note right of memory_adapter
    **Memory Adaptation**
    • CLAUDE.md → Tool-specific memory files
    • AGENTS.md → Universal agent instructions
    • Context preservation
    • Tool-specific customization
end note

@enduml
