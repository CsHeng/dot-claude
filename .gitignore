# Editor directories
/.idea
/.vscode
/.codex
/.cursor
/.github
/.kiro

# user temporary requirements
/requirements

# generated by commands/config-sync
/backup

# macOS system files
.DS_Store

# Sensitive local configuration
/.mise.local.toml

# Claude working directories and runtime files (ignore contents, but .gitkeep preserves structure)
/.claude/settings.local.json
/debug
/file-history
/ide
/projects
/shell-snapshots
/statsig
/todos
/session-env
/history.jsonl
/settings.json*
