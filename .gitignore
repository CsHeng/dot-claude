# Editor directories
.idea/
.vscode/

# macOS system files
.DS_Store

# Sensitive local configuration
.mise.local.toml
settings.json.bak

# Claude working directories and runtime files (ignore contents, but .gitkeep preserves structure)
.claude/
debug/
file-history/
ide/
plugins/
projects/
shell-snapshots/
statsig/
todos/
session-env/
history.jsonl
settings.json*

# Only track specific configuration files
# Keep: CLAUDE.md, rules/, sync-rules.sh, README.md, COMMANDS.md, .mise.toml
# Ignore everything else